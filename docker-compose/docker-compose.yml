services:
  devops-glue:
    build:
      context: ..
      dockerfile: Dockerfile
    image: devops-glue:latest
    container_name: devops-glue
    env_file:
      - ../config/.env
    volumes:
      - ../config/.env:/app/config/.env:ro
      - ../config/docker/nginx.conf:/etc/nginx/sites-available/default:ro
      - ../config/docker/php-fpm.conf:/usr/local/etc/php-fpm.d/zz-custom.conf:ro
      - ../config/docker/supervisord.conf:/etc/supervisor/conf.d/supervisord.conf:ro
      #- ../config/data:/app/config/data
      - ../applog:/data/logs/ci-platform
    ports:
      - "8080:80"
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy

  mysql:
    image: mysql:8.4
    container_name: devops-mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASS:-root123}
      MYSQL_DATABASE: ${DB_NAME:-devops_glue}
    volumes:
      - mysql-data:/var/lib/mysql
    restart: unless-stopped
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  mysql-data:

# ──────────────────────────────────────
# CD Service (separate compose)
# ──────────────────────────────────────
# How to use:
#   1. In devops_cd dir: cp .env.example .env → set DB_HOST=devops-mysql
#   2. cd devops_cd && docker compose up -d (start individually)
#   3. Or uncomment below to start together in the same compose:
#
#   cd-service:
#     build:
#       context: ../../devops_cd
#       dockerfile: Dockerfile
#     container_name: cd-service
#     image: devops-cd:latest
#     env_file:
#       - ../../devops_cd/.env
#     environment:
#       PYTHONUNBUFFERED: "1"
#       HOST: 0.0.0.0
#       PORT: 8081
#       CI_API_URL: http://devops-glue
#       CI_API_TOKEN: dg_xxx   # API token (Service Account / Third Party) Recommendations
#       CI_ADMIN_USER: Service_Account
#       CI_ADMIN_PASS: Service_Password
#     ports:
#       - "8081:8081"
#     restart: unless-stopped
#     healthcheck:
#       test: ["CMD", "python", "-c", "import sys, urllib.request; sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8081/health', timeout=3).status == 200 else 1)"]
#       interval: 30s
#       timeout: 5s
#       retries: 3
#       start_period: 20s
#     depends_on:
#       mysql:
#         condition: service_healthy

