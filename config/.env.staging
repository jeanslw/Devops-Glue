# Staging environment overrides (committed to Git)
# This file holds environment-specific differences only; secrets go in .env.local

APP_DEBUG=true
# API_BASE_URL=http://staging.your-domain.com:8080
