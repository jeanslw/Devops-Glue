[www]
; 与 nginx.conf 中的 socket 路径一致
listen = /run/php/php-fpm.sock
listen.owner = www-data
listen.group = www-data
listen.mode = 0660

; 保留环境变量（关键！否则 .env 注入失效）
clear_env = no

; 性能调优
pm = dynamic
pm.max_children = 20
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 6